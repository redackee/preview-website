# This file is used by GitHub Pages to install Jekyll plugins locally.
# It is not used by GitHub Pages servers, but allows you to preview your site locally with the same plugins.

source 'https://rubygems.org'

gem 'github-pages', group: :jekyll_plugins

#already-installed-as-another-version# gem "jekyll-remote-theme", "~> 0.4.3"
